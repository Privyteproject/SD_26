# Dépendances pour exécuter la suite de tests (CI + local).
# Sous-ensemble de requirements.txt SUFFISANT pour les tests + les outils de test.
# Exclut les dépendances LOURDES, OPTIONNELLES et chargées paresseusement, inutiles en test :
#   - llm-guard / torch       (couche 2 anti-injection : LLMGUARD_ENABLED=false en test ;
#                              la couche 1 regex, elle, reste testée)
#   - sentence-transformers   (embeddings : RAG_EMBED_BACKEND=hash en test)
#   - chromadb                (RAG_VECTOR_BACKEND=memory en test)
# psycopg2 est inutile (les tests tournent sur SQLite).
numpy<2
fastapi>=0.110
pydantic[email]>=2.6
python-jose[cryptography]>=3.3
SQLAlchemy>=2.0
jinja2>=3.1
python-multipart>=0.0.9
Faker>=22,<24
scikit-learn>=1.4
joblib>=1.3
reportlab>=4.0
APScheduler>=3.10
pypdf>=4.2
python-docx>=1.1
redis>=5.0
minio>=7.2
python-keycloak>=3.2.0
pytest>=8
httpx>=0.27
